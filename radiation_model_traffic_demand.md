# 교통 수요 생성 설계 — Radiation Model (+ Selection)

> **문서 목적:** ITS 실시간 데이터가 거의 없는 상황에서, 교통망만으로 각 엣지의 차량 수요를
> 학문적으로 정립된 방법으로 추정하기 위한 설계 정리. Claude Code에서 실제 구현 코드와 대조용.
>
> **결론 한 줄:** 수요는 **4단계 모델(FSM)의 경량 구현**으로 만든다 — 건물 부피를 질량 프록시로
> 쓰고(Generation), radiation model로 OD 행렬을 만들고(Distribution), SUMO 배정으로 엣지 교통량을
> 얻는다(Assignment). 도시 내부 스케일 보정을 위해 **radiation model with selection(파라미터 λ)**을
> 교체 슬롯으로 준비한다.

---

## 0. 전체 파이프라인 (구현 순서)

```
① 격자 존 타일링       전역을 격자로 분할 (크기: §4 참고, [설계 파라미터])
② 존 질량 계산         m_i = 존 i 안 건물 (footprint 면적 × 층수) 합  ← Overpass 폴리곤 재사용
③ OD 행렬 생성         radiation(+λ)으로 T_ij 계산 (§1~2)
④ 트립 비집계          od2trips: 존→엣지 매핑 + 존 내 가중 랜덤으로 개별 엣지에 배치 (§3)
⑤ 경로 배정            duarouter / duaIterate (Wardrop UE 근사) → 엣지별 차량 수 (§3, §5)
⑥ 시뮬레이션·궤적 캐시  단일 실행에서 전 차량 궤적 저장 → 배치 평가에서 재사용 (§5, §6)
```

- **총량(스케일)과 분포(모양)는 분리한다.**
  - 총량 = 사용자 지정 시나리오 파라미터 (HDV 대수와 같은 자리). 절대 예측을 하지 않는다.
  - 분포 = radiation model이 담당. 배치 순위를 가르는 건 이 분포다.
- **방어 원칙:** 우리는 절대 OD 예측기가 아니라 **상대 배치 비교기**다. OD의 구조적 모양(고질량
  존 쏠림 + 개입기회 감쇠 + 방향 비대칭)만 그럴듯하면 배치 A vs B 순위는 OD 오차에 견고하다.
  → latency 문서의 "상대 비교 목적 단순화" 논리와 동일.

---

## 1. Radiation Model — 기본형

### 1-1. 핵심 아이디어

gravity model이 "거리"로 흐름을 감쇠시키는 반면, radiation model은 **출발지와 도착지 사이에 낀
모든 기회(intervening opportunities)**로 감쇠시킨다. 두 목적지가 똑같이 멀어도, 사이에 다른 기회가
많이 낀 쪽으로는 통행이 덜 간다("그늘 효과"). gravity는 이 효과를 원리적으로 못 본다.

- **파라미터가 없다(parameter-free).** 입력은 질량 분포 하나뿐. gravity의 거리 지수 같은 튜닝 노브 없음.
- 유도: 입자의 복사(radiation)·흡수(absorption) 과정 유비. 사람이 출발지에서 "복사"되어 바깥으로
  나가다가, 조건을 만족하는 첫 목적지에 "흡수"된다.

### 1-2. 메커니즘 (job-seeking / job-selecting)

1. 각 사람은 모든 기회에 무작위 만족도 `z`(분포 `p(z)`에서 추출)를 매긴다.
2. 집에서 얻을 수 있는 최고 제안을 기준선으로 삼는다.
3. **가까운 곳부터** 훑다가, 기준선보다 만족도 높은 **첫(=가장 가까운)** 기회에서 멈춘다.
4. 사이에 낀 기회 `s_ij`가 많을수록 도중에 흡수될 확률이 높아져 → 먼 목적지 도달 흐름이 줄어든다.

### 1-3. 수식 (기본형)

트립 확률 (Nature 2012 이산형):

```
                  m_i · m_j
P(i→j) = ─────────────────────────────
          (m_i + s_ij)(m_i + m_j + s_ij)

T_ij = O_i · P(i→j)
```

| 기호 | 의미 | 우리 구현 |
|---|---|---|
| `m_i` | 출발 존 i 질량 | 건물 부피 합 |
| `m_j` | 도착 존 j 질량 | 건물 부피 합 |
| `s_ij` | i 중심 반경 = d_ij 원 안 질량 (단, m_i·m_j 제외) | 반경 내 존들의 건물 부피 합 |
| `O_i` | i의 총 유출 통행량 | 총량 × (m_i / Σm) — origin-constrained |
| `d_ij` | 존 중심 간 거리 | 존 중심 좌표에서 계산 |

`s_ij` 정의 (구현 시 주의):

```
s_ij = Σ_k  m_k  ·  [ d_ik < d_ij ]      (k ≠ i, j)
     = i 에서 j 보다 가까운 모든 존의 질량 합
```

**구현 팁 — 누적합 룩업:** 출발지 i마다 다른 존들을 거리순 정렬하고 질량 누적합을 precompute하면,
`s_ij = (거리 d_ij까지 누적 질량) − m_i − m_j` 로 O(1) 조회. A_seg를 `(segment_id, station_id)` 키로
사전계산한 패턴과 동일.

### 1-4. 세 가지 성질

- **파라미터 없음:** 만족도 분포 `p(z)`가 유도 중 상쇄되어 최종식이 질량 개수로만 결정.
- **비대칭 (`T_ij ≠ T_ji`):** `s_ij`가 출발지 중심이라, 한산한 교외→붐비는 도심은 흐름이 살고
  (교외 주변 s 작음), 도심→교외는 눌린다(도심 주변 s 큼). **통근 방향성이 모델에서 자동 생성** →
  첨두/비첨두 방향을 억지로 안 넣어도 된다.
- **상대 확률만 제공:** 절대 대수는 `O_i`(사용자 총량)로 스케일.

---

## 2. Radiation Model with Selection (RMS) — 도시 내부 보정용

### 2-1. 기본형이 도시 내부에서 실패하는 이유

기본형에는 **조절 손잡이가 없다.** 즉 "사람이 평균 얼마나 멀리 가는가(특성 거리)"가 지형의 기회
밀도만으로 자동 결정된다. 기본 모델은 카운티 스케일(~10 km)에 맞춰 유도되어, **고정된 특성 거리가
도시 내부(~1 km) 스케일과 안 맞는다.** 이것이 "radiation은 도시 내부에서 약하다"의 진짜 원인.
(출처: Masucci et al. 2013; Yang et al. 2014 — intra-city trips에서 성능 저하 보고 다수)

### 2-2. λ가 하는 일

RMS(Simini, Maritan, Néda, *PLoS ONE* 2013, arXiv:1206.4359)는 **제안 수용 확률을 (1−λ)배로 축소**
한다 (λ ∈ [0,1]). 해석 둘:
- 사람이 더 나은 제안을 **확률 (1−λ)로만 받아들인다** (까다로움), 또는
- 사람이 전체 기회 중 **(1−λ) 비율만 인지한다** (정보 제한).

### 2-3. 수식 (RMS)

"가장 가까운 a개 제안을 모두 거절할 확률":

```
기본형:   P_>(a) = 1 / (a + 1)

RMS:      P_>(a) = (1 − λ^(a+1)) / [ (a + 1)(1 − λ) ]
```

- **λ = 0** 이면 기본형(1/(a+1))으로 정확히 환원.
- **λ > 0** 이면 P_>(a)의 중앙값이 큰 a 쪽으로 이동 → **특성 통행거리가 길어진다.**
- **λ = 특성 통행거리 다이얼.** 도시가 작으면 낮게, 광역이면 높게.

트립 확률 (P_> 차분형, 존 j 하나에 대해):

```
a = m_i + s_ij                          # j 보다 가까운 기회 수 (원점 포함)
P(i→j) = P_>(a) − P_>(a + m_j)
```

기본형에서는 이 차분이 §1-3 이산형과 동일해진다 (정규화 상수 차이만 있음).

### 2-4. 실제 λ 값이 1에 가까운 이유 (함정)

US 카운티 데이터 적합값 λ ≈ **0.999988**. 이건 "λ는 항상 1 근처"라는 뜻이 **아니다.**
인구를 일자리 프록시로 쓰면서 `λ_pop = λ_jobs^c` (c ≪ 1)가 되어 프록시 λ가 1로 쏠린 것.
→ **우리도 건물 부피를 프록시로 쓰므로 λ는 1 근처에서 미세 조정하는 형태.** 절대값이 아니라
그것이 만드는 특성거리를 보고 맞춘다.

### 2-5. 파라미터 등급과 방어

| 모델 | 파라미터 | 등급 | 방어 논리 |
|---|---|---|---|
| 기본 radiation | 0개 | [문헌 근거] | 파라미터-프리. 단 도시 내부 특성거리 불일치 |
| RMS | λ 1개 | [설계 앵커] | λ = "이 도시의 통근 특성거리". d_edge를 "엣지 반경"으로 방어한 것과 동일 성격. 지역 스케일로 고정되는 앵커이지 자유 노브 아님 |

### 2-6. 형제 모델 (구현 대안)

- **Extended radiation model (Yang et al. 2014):** 생존분석 틀에서 개입기회 `s_ij`의 영향력을 지수 α로
  조절. OD 닫힌식이 더 깔끔해 도시 내부용으로 실무에서 자주 구현됨.
- RMS(λ, 수용확률 축소)와 extended(α, 개입기회 지수)는 "특성거리 손잡이 하나"라는 점은 같다.
  **구현 편의로는 α-형이 나을 수 있음 → 둘 다 후보.**

### 2-7. 권장 구현 방침

- **MVP:** 기본 radiation(λ=0)으로 시작해 파라미터-프리 서사 유지.
- **슬롯:** distribution을 `gravity / radiation / radiation+selection` **교체형 플러그인**으로 설계
  (warm-start를 greedy/random 슬롯으로 뺀 것과 동일 철학).
- λ(또는 α)는 [설계 앵커]로 노출. 도시 내부 통행거리가 비현실적이면 그 하나만 돌려 맞추고,
  격자 크기에 대한 민감도 분석으로 방어.

---

## 3. OD는 어떻게 정해지나 (개념 명확화)

**핵심: radiation model은 "존 대 존" 흐름을 준다. 개별 지점을 주지 않는다.**

### 3-1. "원"은 OD 영역이 아니다 (오해 방지)

- 반경 `d_ij`인 원은 **개입기회 `s_ij`를 세기 위한 측정용 영역**이다. 출발/도착과 무관.
- 실제 출발·도착은 **존(면적을 가진 격자 칸)**이다. 모델 출력 `T_ij`는 존→존 기대 통행 수.

### 3-2. 존 안 지점은 비집계(disaggregation)로 결정

- 존 안 여러 도로 엣지 중 하나를 (엣지 길이·국소 질량으로 가중해) **무작위로 뽑아** 실제 출발/도착
  엣지로 삼는다. → **SUMO od2trips가 담당.**
- 따라서 "단일 지점 아님. 존 단위 흐름 + 존 내 가중 랜덤 지점."

### 3-3. 전역 차량 분포는 자연히 생긴다 (세 요인 합작)

1. **존을 많이 쪼갠다:** 전역 타일링 + 전체 존쌍 OD 계산 → 트립이 수많은 (i,j)로 흩어짐.
2. **존 내 지점 랜덤:** 같은 존쌍 트립도 서로 다른 엣지에서 출발·도착.
3. **경로가 엣지를 관통:** 트립은 OD 두 존만 정하지만 실제로는 그 사이 경로의 수많은 중간 엣지를
   지나간다. **엣지 차량 수 = 그 엣지를 지나는 모든 트립의 합(배정 결과).**
   → **병목은 OD 끝점이 아니라 고수요 존들 사이 경로가 겹치는 간선·교차로에 발생.**
   → 기존 설계 원칙 "혼잡한 엣지는 OD 종점이 아니라 통과하는 병목" 과 정확히 일치.
   → RSU를 교차로에 놓는 서사와 맞물림.

---

## 4. 격자 존 — 크기 결정 [미확정, §7]

행정동(수 km²)은 도시 내부 수요를 뭉갠다. 격자는 원하는 해상도로 자를 수 있으나 **위아래 하한·상한**
이 있다.

**하한 (더 작게 못 가는 이유):**
- **radiation 통계 유효성:** 존이 너무 작으면 `s_ij`가 존마다 0~1개로 듬성해져 개입기회 감쇠 원리가
  무너진다. 각 존에 기회가 통계적으로 의미 있는 수(수십~수백)로 담겨야 한다.
- **질량 프록시 해상도:** 격자 칸 < 건물 한 채면 질량이 건물 경계 노이즈를 반영. 건물 여러 채가
  한 칸에 들어갈 크기 필요.
- **존→엣지 매핑:** 칸 안에 도로 엣지가 최소 하나 있어야 트립을 붙인다. 너무 잘게 쪼개면 도로 없는
  빈 칸(공원·강)이 급증.

**상한 (더 크게 못 가는 이유):**
- 칸이 **가장 작은 배치 결정 단위(기지국·RSU 커버리지)보다 크면** 수요가 칸 중심에 뭉쳐 배치 순위를
  왜곡. → **칸 < 커버리지** 필수.

**실무 기준:** "평균 블록(건물 몇 채 + 도로 1~2개)이 한 칸에 들되, 기지국 커버리지보다 작게."
도시 격자 연구에서 흔한 **대략 200~500 m**가 위 제약을 대체로 만족. 격자 크기를 [설계 파라미터]로
노출하고 민감도 분석으로 방어.

**참고:** RSU 후보(교차로 노드)와 격자 존(수요 발생 단위)은 분리된 레이어. 격자를 교차로 간격과
비슷하게 잡으면 "한 칸에 교차로 하나"로 해석이 깔끔.

---

## 5. SUMO — 개별 차량 vs 엣지 교통량 (이원 구조)

**SUMO는 차량 개별 위치를 전부 안다.** 엣지 교통량은 파생 집계일 뿐. libsumo로 차량 ID별
`getPosition() / getSpeed() / getRoadID()` 접근 가능. **어느 쪽을 쓸지는 목적에 따라 갈린다:**

```
SUMO 원본: 차량마다 (엣지, 위치 x·y, 속도)  ← 전부 알고 있음
   │
   ├─ [통신 레이어] 개별 좌표 그대로
   │     → 차량–기지국 3D 거리·A_seg → SINR → latency   (target 차량만)
   │        ※ 엣지 교통량으로는 불가능. 같은 엣지 위 차량도 거리·차폐가 제각각.
   │
   ├─ [통신 레이어] 개별 좌표로 best-SINR 기지국 배정
   │     → 기지국별 머릿수 → ρ → L_queue
   │        ※ 엣지 교통량을 직접 큐에 넣지 않음. 개별위치 → 배정 → 기지국별 집계.
   │
   └─ [교통 레이어] 엣지 단위 집계
         → 교통량·속도 → 경로 비용·병목 판정·RL flow field
```

- **통신(latency·큐) = 개별 좌표 필수.** 엣지 집계만으로는 SINR 자체가 성립 안 함.
- **교통·경로 = 엣지 집계.** RL이 보는 flow field(이웃 점유·속도·유입)도 엣지 단위.

**성능 — 궤적 캐시:** 개별 좌표를 매 스텝 전 차량에서 뽑으면 부하가 크다. **단일 SUMO 실행에서 전
차량 궤적(개별 좌표 시계열)을 통째로 저장** → 이후 여러 배치안 평가에서 SUMO 재실행 없이 캐시된
좌표로 SINR·큐만 재계산. **"교통은 한 번, 통신 평가는 여러 번."**

---

## 6. 참고문헌

- [원 radiation] Simini, González, Maritan, Barabási. *A universal model for mobility and migration
  patterns.* Nature 484, 96–100 (2012). arXiv:1111.0586
- [RMS] Simini, Maritan, Néda. *Human Mobility in a Continuum Approach.* PLoS ONE 8(3):e60069 (2013).
  arXiv:1206.4359 — RMS 수식 Eq.(5), λ = 특성거리 다이얼
- [extended α] Yang et al. (2014) — 생존분석 기반 개입기회 지수 α
- [intra-city 한계] Masucci et al. 2013 (arXiv:1206.5735); Nature Sci. Rep. srep05662 (2014)
- [FSM / 4단계] Trip Generation → Distribution → Mode Choice → Assignment (표준 교통수요 프레임워크)
- [Assignment] Wardrop (1952) user equilibrium; Beckmann (1956); Frank-Wolfe (1956).
  SUMO: od2trips → duarouter → duaIterate.py (UE 반복 근사)
- [ODME] 희소 ITS 보정: Willumsen (1978, entropy max), Van Zuylen (1977, info min).
  합성 OD = prior, ITS 지점 = 관측 제약.

---

## 7. 다음에 확정할 것 (Open Decisions)

### (A) 격자 크기 확정
- [ ] 대상 지역의 평균 블록 크기 및 교차로 간격 측정
- [ ] 기지국/RSU 커버리지 반경 확정값과 비교 → 상한 결정 (칸 < 커버리지)
- [ ] 후보 범위(200~500 m) 중 확정, 또는 [설계 파라미터]로 노출 + 민감도 분석 범위 지정
- [ ] 도로 없는 빈 칸 처리 규칙 (질량 0, 트립 제외)
- **결정 기준:** 하한 = radiation `s_ij` 통계 유효성 / 상한 = 배치 커버리지

### (B) 궤적 캐시 데이터 구조
- [ ] 저장 필드: 최소 `(vehicle_id, t, edge_id, x, y, speed)` — 추가 필요 필드?
- [ ] 저장 주기: 매 SUMO 스텝? 다운샘플링(예: 0.1s → 1s)? latency가 target 차량만이면
      target 궤적은 고해상도, 나머지는 배정용 저해상도로 이원화 가능?
- [ ] 저장 포맷: in-memory(numpy/pandas) vs 디스크(parquet/HDF5) — 배치 평가 반복 횟수 대비 I/O
- [ ] 인덱싱: 배치 평가 시 "특정 시각 t에 특정 기지국 반경 내 차량" 조회가 빠르도록
      (시공간 인덱스 / 엣지별 버킷)
- [ ] 캐시 무효화 범위: load-coupling OFF 기준에서 배치 swap이 궤적을 바꾸지 않으므로 궤적 캐시는
      배치와 독립 (교통 1회 실행 후 고정). ON으로 갈 때 재검토 항목.
- **연결:** §5의 "교통 1회, 통신 N회" 구조를 데이터 레이아웃으로 구체화하는 작업

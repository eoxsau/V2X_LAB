# 기지국·RSU 배치 최적화 설계 v2 — SA 골격의 이종(異種) 확장

> **문서 목적:** BS(기지국)와 RSU(노변장치)라는 **속성이 다른 두 종류의 유닛**을 함께 배치
> 최적화하는 SA 엔진의 설계. latency v3.1 정합 반영. Claude Code에서 구현 코드와 대조용.
>
> **v1 → v2 변경:**
> 1. latency v3.1 정합 (값 갱신 + 소거 전제 명시. **인터페이스 변경 없음**)
> 2. **RSU를 이종 유닛으로 추가** — 후보집합·파라미터·swap 규칙 확장
> 3. 개수 정책 **A안 확정** (N_BS, N_RSU 각각 사용자 지정)
> 4. **load-coupling OFF** 기준선 확정 + 연결(association) 규칙 명시
> 5. 교통 수요 입력이 `traffic_demand_design_v2.md`로 대체됨

---

## §0. 결론 한 줄

> 배치 엔진은 **SA(본체) + warm-start + multi-start + delta evaluation** 이라는 v1 골격을
> **그대로 유지**한 채, 유닛 타입을 둘(BS/RSU)로 확장한다. 확장의 핵심은 **타입 보존형 swap** —
> 이것 하나로 `N_BS`, `N_RSU` 두 개수 제약이 v1의 "정확히 N개 제약을 공짜로 지킨다"는 장점을
> 그대로 계승한다.

---

## §1. 선행 문서와의 관계

| 문서 | 이 문서에 주는 것 | 방향 |
|---|---|---|
| `latency_formula_v3_1` | 배치 하나를 채점하는 **비용 함수** (L_total) | 입력 |
| `traffic_demand_design_v2` | 차량이 언제 어디 있는지 (**궤적**) | 입력 |
| **본 문서** | 그 둘을 받아 최적 배치를 탐색 | — |

**의존 방향이 단방향임에 주의.** 배치는 latency와 교통을 소비할 뿐, 그 둘을 바꾸지 않는다.
(load-coupling OFF 기준. §6-3 참조)

---

## §2. latency v3.1 정합 — 인터페이스는 바뀌지 않는다

### 2-1. 무엇이 바뀌고 무엇이 안 바뀌었나

SA가 latency에 요구하는 계약은 하나뿐이다:

```
"이 차량이 이 스테이션에 붙었을 때, 현재 부하에서 L_total이 얼마인가"
```

v3.1이 바꾼 것은 이 함수가 내놓는 **값**이지 **계약**이 아니다.

| 항목 | v3 | v3.1 | SA 영향 |
|---|---|---|---|
| 5G TTI (numerology) | 0.125 ms (num 3) | **0.5 ms (num 1)** | 값만 |
| L_base (5G) | 0.0625 ms | **0.25 ms** | 값만 |
| L_queue 서비스시간 (5G) | 0.125 ms | **0.5 ms** | 값만 |
| α (5G) | — | **76.5** (엣지 1000 m 역산) | 값만 |
| 대역폭 근거 | 전부 [설계 가정] | 4G [표준]·5G [실배치] | 문서만 |

> **결론: SA 골격은 손댈 것이 없다.** 파라미터 테이블 갱신만 필요.

### 2-2. 대신 v3.1이 SA에 부과한 "지켜야 할 전제" 2가지

**⚠ (a) α 소거의 3전제 (v3.1 §5)**
```
① 모든 기지국이 같은 P_tx를 쓸 것      ← RSU 도입으로 흔들림 (§3-2에서 처리)
② α를 항상 역산으로 구할 것            ← 하드코딩 금지
③ 간섭 I = 0 일 것                     ← 11월 cell-free 도입 시 재검토
```

**⚠ (b) load-coupling OFF가 delta evaluation의 전제**
v3.1은 load-coupling ON 시 "혼잡도가 배치에 의존하게 되어 SA 최적화의 캐시 무효화 범위를
재설계해야 한다"고 명시. → delta evaluation의 국소성은 **OFF일 때만** 성립 (§6-3).

### 2-3. Outage 처리와 SA 호환성

```
SINR < −6 dB  ⇒  L_total = L_OUTAGE = 1000 ms  (유한 페널티)
```
무한대(∞)를 쓰면 SA의 비용 차분 계산이 깨지고, MCS 0으로 억지 연결시키면 최적화가 음영지역을
덜 무서워한다. **유한한 큰 상수**는 두 문제를 모두 피하며 음영지역 면적에 비례해 배치안이
벌점을 받게 한다. → v3.1의 이 설계 결정이 SA와 직접 맞물리는 지점.

---

## §3. RSU — 첫 이종(異種) 유닛

### 3-1. RSU는 "작은 BS"가 아니다

| | BS (기지국) | RSU (노변장치) |
|---|---|---|
| 설치 위치 | **건물 옥상** | **도로 교차로** |
| 후보 집합 | 건물 폴리곤 중심 | 교차로 노드 |
| 안테나 높이 | 높음 (h ≈ 24 m) | 낮음 (h ≈ 5 m, 잠정) |
| 커버리지 (d_edge) | 큼 | **작음** |
| 용량 (C_tech) | 큼 | **작음** |

두 후보 집합은 **서로소(disjoint)** 다. 건물과 교차로는 겹치지 않는다.

### 3-2. ⚠ α 소거 전제 ①과의 충돌 — 정확히 무엇이 깨지는가

RSU는 P_tx가 BS와 다르므로 "모든 기지국이 같은 P_tx"라는 전제를 건드린다. 그러나 정확히 따지면:

**✓ 링크 단위 SINR은 안 깨진다.**
BS든 RSU든 **각자 자기 d_edge로 α를 역산**하면, 자기 P_tx와 noise_floor는 자기 식 안에서 상쇄된다:
```
SINR_type(d) = SINR_min + PL_type(d_edge_type) − PL_type(d) − A_seg
```
→ 전제 ②(항상 역산)만 지키면 타입별로 독립 성립. **개별 링크 지연이 P_tx 절대값과 무관하다는
  방어선은 유지된다.**

**✗ 깨지는 것은 "타입 간 비교"다.**
차량이 BS와 RSU 중 하나를 고를 때 비교하는 두 SINR은 각자 **"자기 셀 엣지보다 몇 dB 유리한가"**
라는 **상대 마진**이다. 동종만 있을 때는 d_edge가 같아 마진 순위 = 수신세기 순위였으나, 이종이
섞이면 타입마다 오프셋이 달라 **마진 최대 ≠ 수신세기 최대**가 된다.

즉 실제 물리에서 BS–RSU의 세기 관계를 정하는 것은 P_tx 차이인데, α를 타입별로 독립 역산하면서
그 차이를 버리고 **d_edge 비율(커버리지 비율)로 대체**한 것이다.

**→ 소거 증명이 "깨진" 것이 아니라, 실제 자유도가 방어 가능한 자리로 이동한 것.**
   v3.1이 세대 간 d_edge를 "**2:1 비율이 핵심**"이라고 방어한 것과 **동일한 논리 구조.**

> **설계 규칙: 타입 간 결합 파라미터는 딱 하나만 도입한다** (커버리지·용량 비율, 또는 등가로
> P_tx 격차 ΔP). 그리고 세대 비율과 같은 방식으로 방어한다.

### 3-3. ⚠ 커버리지 축소를 두 경로로 이중 지정하지 말 것

RSU의 작은 커버리지를 표현하는 방법이 두 가지 있다:

| 경로 | 방법 | 효과 |
|---|---|---|
| (a) | d_edge를 작게 잡아 α로 흡수 | 엣지 반경 직접 지정 |
| (b) | 안테나 높이 h_RSU를 낮춰 d_BP 축소 | 4.0 기울기가 일찍 시작 → 빨리 감쇠 |

**둘 다 적용하면 같은 "작은 커버리지"를 두 채널로 두 번 말하는 것**이 되어, 값이 안 맞으면
β/A_seg 이중계산과 동일한 role-overlap 오류가 발생한다.

**→ 권장: (b)를 물리적 원인으로 두고 d_edge는 그 결과로 유도.**
h_RSU를 낮게(≈5 m) 잡으면 d_BP·A_seg가 자연스럽게 따라 나온다. 높이라는 하나의 물리량이
커버리지·차폐·엣지를 **일관되게** 지배하는 구조가 깔끔하다.

### 3-4. RSU의 물리적 특성 — 도로 축을 따라 길쭉한 커버리지

RSU는 낮게 설치되므로 3D ray-casting에서:
- 도로 축을 벗어난 방향 → 건물에 많이 막힘 (A_seg 큼)
- 도로를 따라서는 → 뻥 뚫림 (LOS)

**→ RSU 커버리지는 방사형이 아니라 도로 축을 따라 길쭉하게 형성된다.**

**중요: 이것은 기존 A_seg 기계(건물 수 × 12 dB, `(segment_id, station_id)` 사전계산)에
`h_RSU`만 낮게 넣으면 그대로 재현된다. 새 전파 모델이 필요 없다.**

**⚠ 스탬프:** 엄밀히는 UMa LOS가 옥상 매크로 전용이라 street-level RSU에는 UMi가 더 적합.
상대비교 MVP에서는 높이만 조정해 재사용하고, **11월에 UMi 도입 검토**를 열어둔다.

### 3-5. RSU를 넣는 서사 — "교차로 = 정체 병목"

"BS보다 싸서"는 약한 논거다. 강한 서사:

> RSU는 **교차로에 놓이고, 교차로가 바로 spillback 병목이 발생하는 지점**이다.
> 즉 RSU는 "정체가 뭉치는 바로 그 자리에 저지연 용량을 꽂는" 유닛이다.

- 라우팅 RL의 논지("병목 전파 동역학을 읽는다")와 직결
- `traffic_demand_design_v2` §8-1의 결론("혼잡한 엣지는 통과 병목")과 맞물림
- 멀리 있는 BS는 병목 교차로 차량에게 SINR도 나쁘고 큐도 길지만, RSU는 그 자리에서 도로를 따라
  뻗는 LOS로 저지연을 제공

---

## §4. 개수 정책 — A안 확정

### 4-1. 두 안의 비교

| | **A안 (채택)** | B안 (기각/보류) |
|---|---|---|
| 방식 | 사용자가 `N_BS`, `N_RSU`를 **각각 지정** | 총예산만 주고 BS/RSU 믹스까지 SA가 결정 |
| swap | **타입 보존형** → 개수 제약 자동 유지 | 타입 변경 move 필요 → 개수 불변식 깨짐 |
| 추가 파라미터 | 없음 | **BS/RSU 상대 비용 모델** 필요 |
| 판정 | **MVP 채택** | 11월 결선에서 "믹스 최적화"를 셀링포인트로 쓸 경우 재검토 |

### 4-2. A안이 v1의 핵심 장점을 계승하는 방식

v1 SA 채택 근거 1순위가 **"'정확히 N개 선택' 제약을 공짜로 지킨다"** 였다. 이종에서도:

```
BS 슬롯은 BS 후보끼리만 교체
RSU 슬롯은 RSU 후보끼리만 교체
   ↓
N_BS, N_RSU 두 제약이 둘 다 자동으로 유지됨 (별도 제약 처리 코드 불필요)
```

> **타입 보존형 swap = 이종 확장의 핵심 설계 결정.** 이것 하나로 v1 장점이 그대로 확장된다.

---

## §5. 배치 하나를 채점하는 4단계 (load-coupling OFF)

### 5-1. 상태(state)의 정의

```
배치 = { 선택된 건물 N_BS개 } ∪ { 선택된 교차로 N_RSU개 }
```
각 스테이션은 타입에 따라 `(d_edge, C_tech, 높이, α)` 를 다르게 보유.
A안이므로 두 개수는 실행 내내 고정.

### 5-2. 채점 파이프라인 — 한 방향으로만 흐른다

**OFF의 핵심 성질: 되먹임 고리가 없어 반복 수렴 계산이 필요 없다.**

```
(1) 연결(association) — 신호만 보고 붙는다
      각 차량이 마진 최대 스테이션에 연결 (best-SINR)
      ※ 큐(혼잡)는 이 단계에서 보지 않음
            ↓
(2) 머릿수 → 혼잡도 ρ
      스테이션별 접속 차량 수 ÷ C_tech  (최대 0.99)
      ※ RSU는 C_tech가 작아 같은 머릿수라도 ρ가 빨리 1에 근접
            ↓
(3) 차량별 지연 L_total
      L_base(타입별) + L_transmission(그 차량 SINR→MCS) + L_queue(붙은 스테이션 ρ)
      ※ SINR < −6 dB 이면 세 조각 대신 L_OUTAGE = 1000 ms
            ↓
(4) 배치 비용
      차량별 L_total을 교통 수요로 가중 합산 → 이 배치의 비용 (스칼라)
```

**한 줄 요약:** `배치 → 연결 → 머릿수 → ρ → 지연` 이 한 줄로 흐른다.
ON이었다면 "혼잡하니 딴 데로 도망 → 머릿수 변화 → 다시 혼잡 변화"가 물고 돌아 **고정점 반복**이
필요하지만, OFF는 그 고리가 끊겨 있어 **한 번 훑으면 끝**.

### 5-3. 연결 규칙 — best-SINR (MVP 기준선)

각 차량은 **마진(자기 셀 엣지 대비 우위) 최대** 스테이션에 연결한다. BS/RSU를 이 마진으로 비교.

**이것이 근시안적임에도 MVP에 충분한 이유:**
RSU가 신호가 좋아 차량을 많이 끌어당기지만 `C_tech`가 작아 ρ가 0.99로 포화되면 `L_queue`가
폭발 → **그 배치의 비용이 올라가 → SA가 알아서 그런 배치를 피한다.**
즉 개별 연결이 근시안적이어도 **배치 수준에서 큐 페널티가 자기교정**을 수행한다.

---

## §6. SA 엔진 — v1 골격 유지 + 이종 확장

### 6-1. 실행 흐름 (v1 §2-1 확장)

첨두·비첨두 각 시나리오마다 독립 수행:

```
1. 입력 준비   후보 건물(BS) + 후보 교차로(RSU), 차량 궤적, N_BS·N_RSU, 기술(4G/5G/6G)
2. 초기해 생성 (병렬)  이종 greedy 궤적 1~2개 + random 궤적 1~2개
3. SA 탐색 (병렬)      타입 보존형 swap + delta evaluation
4. 최선 선택           모든 궤적 중 최소 비용 배치 채택
5. 비교·출력           첨두 vs 비첨두 나란히 제시
```

### 6-2. warm-start — 이종 greedy로 자연 확장

v1의 greedy forward selection:
> 빈 배치에서 시작해, 매 단계마다 비용을 가장 많이 낮추는 건물을 하나씩 추가

**이종 확장 — 한 줄만 바뀐다:**
```
매 단계, "아직 쿼터가 안 찬 타입 중" 가장 이득 큰 스테이션을 추가
   (BS 쿼터 N_BS, RSU 쿼터 N_RSU가 각각 찰 때까지)
```

**계승되는 것:**
- "greedy = 학습 없는 RL" 서사 (constructive RL과 골격 동일)
- `random → greedy → RL` 3단 비교로 순수 학습 기여만 분리 (v1 Table 10)
- multi-start 안전장치 (**warm-start만으로 채우지 않는다** — v1 Table 7 규칙 유지)

**⚠ 결합(joint) vs 단계(staged):**

| 방식 | 설명 | 판정 |
|---|---|---|
| **joint (원칙)** | 두 타입 슬롯을 섞어 **한 SA로** 동시 최적화 | ✓ 채택 |
| staged | BS 먼저 깔고 그 위에 RSU | warm-start/greedy 버전으로만 |

staged는 구현이 쉽지만 **BS 위치가 RSU 가치를 바꾸는 상호작용을 못 본다** — greedy가 근시안인
것과 정확히 같은 약점. 따라서 staged는 joint SA의 **출발점 생성기**로 자리매김.

### 6-3. delta evaluation — 국소성이 유지되는 이유와 그 한계

**살아남는다.** best-SINR 연결 하에서 스테이션 하나를 swap하면 연결이 바뀌는 차량은 **그 근처
뿐**(신호로만 붙으므로 기하학적으로 국소적). 오히려 RSU는 커버리지가 작아 **swap의 영향 반경이
더 작아** delta 이득이 크다.

**⚠ 미묘한 지점 — ρ는 스테이션 단위 공유값이다:**
```
어떤 차량이 A를 떠나 B로 가면
  → A와 B에 "남아있던, 움직이지 않은" 차량들의 L_queue까지 전부 재계산 필요
```
다행히 OFF에서는 머릿수가 바뀌는 스테이션이 (빠진 것 + 들어온 것 + 고아 차량을 주워담는 이웃
한둘) 정도로 **소수**라, "영향 스테이션 몇 개 + 그 위 차량만 재큐잉"으로 여전히 저렴하다.

> **핵심: "바뀐 부분만"의 "부분"이 OFF에서는 작게 유지된다.** ON이면 이 재큐잉이 도미노로
> 번져 국소성이 깨진다 → v3.1이 경고한 "캐시 무효화 범위 재설계".

### 6-4. multi-start / SA 본체

v1과 동일. 변경 없음. (v1 §3-1, §3-3, Table 3~5, Table 7 그대로 적용)

---

## §7. 배치 원리 — SA가 실제로 최적화하는 것

### 7-1. 지렛대는 셋뿐

α 소거(P_tx·noise_floor 제거) + 간섭 I=0 이므로, 최적화가 실제로 보는 것은:

```
① 2-Slope 곡선 위에서의 거리 비율
② 건물 차폐 개수 (A_seg)
③ 머릿수 / 용량 (ρ)
```

**최적화 지형이 이 셋으로 깔끔하게 축소된다는 것이 α 소거의 실질적 값어치.**

### 7-2. 서로 당기는 두 힘의 균형

| 힘 | 방향 | 메커니즘 |
|---|---|---|
| **신호 힘** | 수요 밀집지로 **모으라** | 가까이·가시선 확보 → 거리손실·A_seg ↓ → SINR ↑ → 전송 빠름, outage 회피 |
| **용량 힘** | 한 곳에 몰지 말고 **퍼뜨리라** | 한 스테이션에 몰리면 머릿수 > C_tech → ρ→0.99 → L_queue 폭발 |

> **은행 비유:** 손님 많은 곳에 창구를 몰면 다들 가깝지만(신호 좋음) 그 창구가 터지고(큐 폭발),
> 창구를 흩으면 안 터지지만 멀리서 오는 손님이 생긴다(엣지 차량, 느린 전송).
> **SA는 이 사이 지점을 찾는다.**

### 7-3. BS와 RSU의 자연스러운 역할 분업

| 유닛 | 역할 | 메커니즘 |
|---|---|---|
| **RSU** | 교차로 무리 전용 **저지연 패치** | 교차로 한복판 차량은 RSU에 매우 가깝고 도로 축 LOS라 마진이 큼 → best-SINR에서 **안쪽 무리를 자연히 흡수**. 짧은 거리라 전송 빠르고 outage 없음. 단 C_tech가 작아 일정 수 넘으면 포화 |
| **BS** | 광역 커버리지 + **용량 백스톱** | 교차로에서 200 m쯤 벗어나면 RSU 마진이 작아지고 BS 마진이 커져 **바깥 링을 BS가 흡수**. 흩어진 차량 전체를 덮고 RSU 오버플로를 수용 |

> **SA의 실제 결정:** "한정된 RSU를, **안쪽 무리가 C_tech 안에 들어오면서 저지연 이득이 가장 큰
> 교차로**에 배치하고, BS로 나머지와 용량을 메운다."

### 7-4. ⚠ 정직한 한계 — OFF의 대가

best-SINR 연결이므로 차량은 **포화된 RSU에도 그냥 붙는다.** 살짝 먼 BS에 여유가 있어도 "신호가
더 좋다"는 이유로 터진 RSU에 남아 큐를 뒤집어쓴다.

- 모델은 이를 그 배치의 `L_queue` 페널티로 드러내므로, SA는 "**RSU를 감당 못 할 수요지엔 놓지
  않는**" 방식으로 회피한다.
- 그러나 "RSU는 감당할 만큼만 받고 나머지는 BS로 흘려보내기" 같은 **지능적 분산은 못 한다.**
  그것은 부하 인지 연결(ON)이라야 가능.

> **OFF의 배치 원리는 과밀을 "안 만드는" 방식으로 피하지, "지능적으로 나누는" 방식이 아니다.**
> MVP의 상대 비교에는 충분하며, 이 한계가 곧 11월 ON 확장의 명분이 된다.

---

## §8. load-coupling — OFF/ON의 의미

### 8-1. 지금 큐 모델에 깔린 가정

```
ρ = 접속 차량 수 ÷ C_tech,   L_queue = TTI × ρ/(1−ρ)
```
숨은 가정: **모든 차량의 서비스 시간이 동일(1 TTI 슬롯)** → ρ가 오직 **머릿수**로만 결정.

**반례는 v3.1 §3-3에 이미 있다.** 같은 1 Mbit라도:
- 신호 최상(MCS 28): 1.69 ms
- 신호 최악(MCS 0, 엣지): 42.66 ms → **창구 점유 시간 25배 차이**

### 8-2. OFF와 ON

| | **OFF (MVP)** | **ON (11월 확장)** |
|---|---|---|
| 서비스 시간 | 1 TTI 슬롯 고정 | 그 차량의 **실제 L_transmission** |
| ρ 결정 | 머릿수만 | 엣지 차량은 25배로 기여 |
| 연결 | best-SINR | 부하 인지 (고정점 반복) |
| 계산 | 가벼움 (1-pass) | 무거움 (수렴 반복) |
| delta | 국소성 유지 | **캐시 무효화 범위 재설계 필요** |

### 8-3. 왜 "coupling(결합)"인가

**누가 엣지 차량이 되는지를 정하는 것이 바로 배치**다. ON을 켜면 고리가 생긴다:

```
배치 → 각 차량 신호 품질 → 각 차량 창구 점유 시간 → 셀 혼잡도
     → (혼잡하면 딴 셀로 도망) → 연결 변화 → 다시 혼잡도 변화 → …
```
혼잡도와 배치·연결이 **서로 물고 도는 상호 의존** = coupling.
OFF에서는 ρ가 머릿수만 보므로 이 고리가 끊겨 있다.

### 8-4. RSU가 ON의 가치를 키운다

RSU는 `C_tech`가 작아 어차피 빨리 포화되는데, ON을 켜면 "RSU가 엣지 차량을 물고 있으면 이중으로
나쁘다"는 효과가 더해진다. **이종 용량이 섞일수록 "가까운 RSU가 포화면 좀 먼 BS로 우회"라는
부하 인지 연결이 의미를 갖는다.**

> RSU 도입은 v3.1의 ON 확장 결론을 **바꾸는 것이 아니라 강화**한다.

---

## §9. 설계 결정 요약

| 항목 | 결정 | 근거 |
|---|---|---|
| 최적화 방법 | **SA (본체)** | 개수 제약 공짜 유지, 비선형·불연속 지형, 단순 구조 (v1 계승) |
| 유닛 타입 | **BS + RSU (이종)** | RSU = 교차로 = spillback 병목 지점에 저지연 패치 |
| 후보 집합 | BS=건물 옥상 / RSU=교차로 노드 (서로소) | 물리적 설치 제약 |
| **개수 정책** | **A안 — N_BS, N_RSU 각각 사용자 지정** | 타입 보존형 swap으로 두 제약 자동 유지 |
| **swap 규칙** | **타입 보존형** | 이종 확장의 핵심. 개수 불변식 자동 보장 |
| RSU 커버리지 축소 경로 | **h_RSU를 낮게 → d_BP·A_seg 유도** (d_edge는 결과) | 이중 지정 방지 (role-overlap) |
| 타입 간 결합 파라미터 | **하나만** (커버리지·용량 비율) | 세대 간 2:1 비율 방어 논리와 동일 구조 |
| 연결(association) | **best-SINR** | OFF에서 연결↔큐 결합을 끊음. 배치 수준 자기교정으로 충분 |
| **load-coupling** | **OFF (기본)** | 1-pass 채점, delta 국소성 유지. ON은 11월 |
| 최적화 방식 | **joint (두 타입 동시)** | staged는 BS↔RSU 상호작용을 못 봄 → warm-start 용도로만 |
| warm-start | 이종 greedy (쿼터 미달 타입 중 최선) | v1 greedy forward selection의 자연 확장 |
| multi-start | **필수** (warm-start 1~2 + random 1~2) | v1 규칙 유지. 단일 궤적 금지 |
| delta evaluation | 포함 | OFF 기준 국소성 성립. RSU는 영향 반경이 더 작아 이득 큼 |
| 첨두/비첨두 | 각각 독립 최적화 | v1 해석 A 유지. 파이프라인 전체를 한 번씩 |
| Outage | 유한 페널티 1000 ms | ∞는 차분 계산을 깨고, MCS 0 클램프는 음영지역을 안 무서워함 |
| RL | 슬롯만 (초기화 교체) | 출발점 편차 측정 후 도입 판단 (v1 Table 10 유지) |

---

## §10. 다음에 확정할 것 (Open Decisions)

### (A) RSU 물리 파라미터 확정
- [ ] `h_RSU` 확정 (≈5 m?) → d_BP 산출 → d_edge 유도
- [ ] `C_tech,RSU` 확정 (BS 대비 비율로 지정 — 절대값보다 비율이 방어 가능)
- [ ] RSU 기술 세대 처리: BS와 같은 세대? 별도 지정?
- **결정 기준:** 타입 간 결합 파라미터를 **하나로** 유지 (§3-2)

### (B) RSU 후보 교차로 추리기
- [ ] 모든 교차로가 후보인가, 아니면 사전 가지치기?
- [ ] 후보 수가 BS 후보(건물)보다 훨씬 적을 것 → 탐색 공간 비대칭이 SA에 미치는 영향
- [ ] 격자 존과의 관계 (`traffic_demand_design_v2` §3-2 참고: "한 칸에 교차로 하나"로 잡으면 해석 깔끔)

### (C) 목적함수 세부
- [ ] 차량별 L_total 합산 방식: 총합 vs 수요가중 평균
- [ ] 타깃 차량만 평가 vs 전 차량 평가 — 현재 latency는 타깃 차량 한정 (§11 참조)
- [ ] outage 차량 비중을 별도 지표로 뽑을지

### (D) 궤적 캐시 연동
- [ ] `traffic_demand_design_v2` §14-(C)와 공통 항목
- [ ] **배치 평가에서 필요한 조회 패턴:** "시각 t에 스테이션 s 반경 내 차량" → 시공간 인덱스 설계
- [ ] OFF 기준에서 배치 swap이 궤적을 바꾸지 않으므로 **궤적 캐시는 배치와 독립** (교통 1회 실행
      후 고정). ON 전환 시 재검토

### (E) SA 하이퍼파라미터
- [ ] 초기 온도 / 냉각 스케줄 / 반복 횟수
- [ ] multi-start 궤적 수 (CPU 코어 수 기준)
- [ ] 수렴 판정 조건
- [ ] **출발점 편차 측정 프로토콜** (v1 Table 10 — RL 도입 판단 근거)

### (F) 11월 확장 재검토 항목
- [ ] load-coupling ON → 부하 인지 연결 + 고정점 반복 + 캐시 무효화 재설계
- [ ] cell-free massive MIMO → α 소거 전제 ①·③ 재검토
- [ ] RIS 배치 (A_seg를 고정 페널티에서 가변 변수로 전환)
- [ ] street-level RSU에 UMi 경로손실 모델 도입 검토 (§3-4 스탬프)
- [ ] B안(믹스 최적화) 재검토 — 상대 비용 모델 필요

---

## §11. 구현 시 주의점 모음

**⚠ 하드코딩 금지 목록**
- `α` — 반드시 d_edge에서 역산 (전제 ②). 문헌값 직접 사용 시 소거 증명 붕괴
- `P_tx`, `noise_floor` — 결과에서 소거되므로 값 자체는 임의여도 되나, **타입별로 일관되게** 사용

**⚠ 이중계산 체크리스트** (이 프로젝트에서 반복 발생한 오류 유형)
| 지점 | 올바른 분리 |
|---|---|
| β vs A_seg | β=거리·지면반사 / A_seg=건물 차폐 |
| RSU d_edge vs h_RSU | 높이 하나로 통일, d_edge는 유도 |
| L_base vs L_transmission | L_base=대기만 / L_transmission=전송 전체 |

**⚠ 타입 보존 불변식 검증**
swap 후 항상 `count(BS) == N_BS` 및 `count(RSU) == N_RSU` 를 assert. A안의 전제가 코드에서
깨지지 않는지 방어적으로 확인.

**⚠ delta evaluation 정합성 검증**
delta로 갱신한 비용과 전체 재계산 비용이 일치하는지 주기적으로 대조(예: 100 swap마다 1회).
ρ 재계산 누락(§6-3의 "남아있던 차량")이 가장 흔한 버그 지점.

---

## §12. 참고

- `latency_formula_v3_1` — L_total 계산, α 역산, 소거 3전제, outage 처리
- `traffic_demand_design_v2` — 차량 궤적 생성, 교차로 병목 발생 메커니즘
- `base_station_SA_design` (v1) — SA/warm-start/multi-start/delta evaluation 개념 설명 원본
- 3GPP TR 38.901 — UMa (BS) / UMi (RSU 확장 검토용)

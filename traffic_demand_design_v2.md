# 교통 수요 생성 설계 v2 — Radiation Model + KTDB 접지

> **문서 목적:** ITS/VDS 실시간 데이터가 거의 없는 상황에서, 도로망과 건물 데이터만으로 각 도로의
> 차량 수요를 학문적으로 정립된 방법으로 생성하기 위한 설계. Claude Code에서 구현 코드와 대조용.
>
> **v1 → v2 변경:** 친구 설계(TFR-CPA)의 데이터 계층 통합(표준노드링크·VDS·KTDB AADT), 총 교통량을
> 사용자 입력에서 **KTDB 기반 시스템 산출 + 사용자 배율**로 변경, 격자 구성 규칙(걸친 건물·MAUP)
> 추가, SUMO 운영 설정(step-length·teleport·신호) 추가.

---

## §0. 한 문장 요약과 비유

> **건물이 어디에 얼마나 있는지를 보고 → 차가 어디서 출발해 어디로 갈지를 계산하고 →
> 실제 도로에 굴려서 → 각 도로의 차량 수를 얻는다.**

| 물 비유 | 우리 교통 |
|---|---|
| 샘물이 솟는 곳 | 건물 많은 곳 (사람이 출발) |
| 물이 고이는 웅덩이 | 사무실·상가 (사람이 도착) |
| 물길 | 도로망 |
| 물길이 좁아져 넘치는 곳 | **정체(병목)** |

**핵심 원칙: 정체를 직접 지정하지 않는다.** 샘과 웅덩이만 정하고 물을 흘리면 좁은 목에서 저절로
넘친다. 정체가 **결과로 발생**해야 (a) 재현 가능하고 (b) 선행 신호가 있고 (c) 조건이 바뀌면 정체
위치도 따라 바뀐다. 이 세 가지가 있어야 RL이 학습할 대상이 존재한다.

**왜 이 방식인가:** 원래 ITS 실시간 데이터로 도로별 교통량을 받으려 했으나 데이터가 거의 없어
전부 0으로 잡힘. → 관측을 포기하고 **원인(건물 → 사람 → 통행)에서 재생성**하는 방향으로 전환.

---

## §1. 데이터 인벤토리

| 데이터 | 출처 | 용도 | 가용성 |
|---|---|---|---|
| **도로망** (노드·링크, 차로수, 제한속도, road_rank) | **표준노드링크** ※친구 설계 채택 | SUMO 네트워크 | 항상 (전국) |
| **건물 폴리곤** (바닥면적, 층수) | Overpass API / VWorld | **존 질량 = 수요 근원** | 항상 |
| **KTDB 등급별 AADT** | 국토부 2022 ※친구 설계 채택 | **기준 총량 N\* 산출** | 항상 (전국 통계) |
| **VDS/ITS 실측** (volume, occupancy) | 국가교통정보센터 ※친구 설계 채택 | N\* 국소 보정 + 홀드아웃 검증 | **지역 의존 (없을 수 있음)** |
| **수요 배율** | 사용자 입력 (기본 1.0) | 시나리오 탐색 | 항상 |

**두 가지 메모:**
- 건물 폴리곤은 통신 계산의 `A_seg`(건물 차폐)용으로 이미 Overpass에서 수신 중 → **재활용, 추가
  비용 없음.**
- 도로망을 OSM → **표준노드링크로 교체**. 국내에서 차로수·제한속도·도로등급 품질이 우수하고,
  **배정(assignment) 품질이 네트워크 속성 정확도에 직접 의존**하므로 (차로수·자유속도 → 링크 용량
  → 병목 위치 결정) 순수 이득. radiation과 충돌하지 않음.

---

## §2. 전체 파이프라인

```
[건물 데이터]              [도로망 데이터]           [KTDB 통계]        [VDS 실측(선택)]
 Overpass/VWorld            표준노드링크              등급별 AADT        volume+occupancy
      │                          │                        │                   │
      ▼                          │                        ▼                   ▼
① 격자 존 타일링 (§3)             │                  ⑤ 기준 총량 N* 산출 (§5) ◄──국소 보정
      ▼                          │                        │
② 존 질량 m_i = 건물 부피 합 (§4) │                        │
      ▼                          │                        ▼
③ 존별 출발량 O_i = N × m_i/Σm ◄──────────────────── N = N* × 사용자 배율
      ▼                          │
④ Radiation Model → OD 행렬 T_ij (§6)
      ▼                          ▼
⑥ od2trips: 존 내 도로에 가중 랜덤 배치 → 개별 트립 (§7)
      ▼
⑦ duarouter/duaIterate: 경로 배정 (Wardrop UE 근사) (§8)
      ▼
⑧ SUMO 주행 → 도로별 교통량 + 차량별 궤적
      ▼
⑨ 검증: VDS 홀드아웃 + KTDB 등급별 비율 상식체크 (§9)
      ▼
   [최종] 차량별 좌표 → 통신 계산 / 도로별 집계 → 경로·RL (§11)
```

---

## §3. 격자 존 구성 [일부 미확정 → §14]

### 3-1. 왜 격자인가

건물 한 채씩 다루면 수만 개라 감당 불가. 지도를 바둑판처럼 잘라 한 칸을 하나의 "지역(존)"으로
취급한다. 행정동(수 km²)은 도시 내부 수요를 뭉개므로 **격자**를 사용.

### 3-2. 격자 크기 — 상하한 논리 [미확정]

**하한 (더 작게 못 감):**
- **radiation 통계 유효성:** 존이 너무 작으면 개입기회 `s_ij`가 존마다 0~1개로 듬성해져 감쇠 원리가
  무너짐. 각 존에 기회가 통계적으로 의미 있는 수로 담겨야 함.
- **질량 프록시 해상도:** 칸 < 건물 한 채면 질량이 건물 경계 노이즈를 반영.
- **존→엣지 매핑:** 칸 안에 도로 링크가 최소 1개 필요. 너무 잘면 도로 없는 빈 칸 급증.

**상한 (더 크게 못 감):**
- 칸이 **가장 작은 배치 결정 단위(BS·RSU 커버리지)보다 크면** 수요가 칸 중심에 뭉쳐 배치 순위 왜곡.
  → **칸 < 커버리지** 필수.

**실무 후보:** 200~500 m. 격자 크기를 [설계 파라미터]로 노출하고 민감도 분석으로 방어.

**참고:** RSU 후보(교차로 노드)와 격자 존(수요 발생 단위)은 분리된 레이어. 격자를 교차로 간격과
비슷하게 잡으면 "한 칸에 교차로 하나"로 해석이 깔끔.

### 3-3. 격자에 걸친 건물 처리 ★신규

**선택지 3개 중 하나는 금지:**

| 방식 | 설명 | 판정 |
|---|---|---|
| **(a) 대표점 배정** | 건물 대표점이 들어간 칸에 건물 **전체**를 배정 | **✓ 기본 채택** |
| **(b) 면적 비례 분할** | 40:60으로 걸쳤으면 부피도 40:60 배분 | ✓ 대형 건물만 |
| **(c) 겹치면 양쪽 다 계산** | 조금이라도 겹치면 그 칸의 건물로 100% 계산 | **✗ 금지** |

**⚠ (c) 금지 사유 — 이중계산.** 걸친 건물이 양쪽 칸에 100%로 들어가면 전체 질량 합 `Σm`이 부풀고,
`O_i = N × m_i/Σm` 배분이 통째로 왜곡됨. 같은 건물을 두 번 세는 것으로, β/A_seg 역할 분리나
친구 설계의 α_rank 이중계산과 **동일 계열의 오류.** 원천 금지 규칙으로 명시.

**(a)를 기본으로 하는 근거 — 오차 규모 계산:**

건물 한 변 20 m, 격자 500 m 기준:
```
한 축에서 경계에 걸릴 확률 ≈ 20/500 = 4%
두 축 → 약 8% 의 건물이 경계에 걸침
걸친 건물의 오배정 부피 ≈ 해당 건물의 1/4
```

| 격자 크기 | 걸치는 건물 비율 | 전체 질량 오차 |
|---|---|---|
| 500 m | ~8% | **~2%** |
| 200 m | ~20% | **~5%** |

- 이미 안고 있는 오차가 훨씬 큼: **건물 부피로 사람 수를 대신하는 것 자체**가 근사(같은 부피라도
  아파트 vs 창고는 통행 발생이 몇 배 차이). 수십 % 오차 위에서 2%를 정밀 처리하는 것은 가짜 정밀도.
- **radiation은 이 오차에 특히 강함:** 핵심 계산 `s_ij`가 "반경 내 존들의 질량 **누적합**"이라,
  경계에서 A칸이 손해 보면 B칸이 이득 보아 합에서 상쇄됨.

**⚠ 구현 함정:** 중심점(centroid)을 그대로 쓰면 안 됨. ㄱ자·ㄷ자 건물은 **중심점이 폴리곤 밖에
떨어질 수 있음.** → shapely `representative_point()` 사용 (폴리곤 내부 점 보장).

**혼합 규칙 (권장):**
```
건물 바닥면적 > 격자 면적의 5%  →  면적 비례 분할 (b)
그 외                          →  대표점 배정 (a)
```
대형 쇼핑몰·대학 캠퍼스·대단지는 한 변 100~200 m로 격자와 맞먹어 대표점 오차가 크고 질량도 큼.
수가 적어 분할 비용이 거의 없으므로 비용 대비 효과 최선.

### 3-4. 격자선을 어디에 긋는가 — MAUP ★신규

격자를 반 칸(예: 250 m) 옆으로 밀면 존 구성이 통째로 바뀌고 `m_i`도 `s_ij`도 달라진다.
이는 지리학의 **MAUP (Modifiable Areal Unit Problem)** 로 알려진 문제:

- **스케일 효과:** 칸 **크기**를 바꾸면 결과가 바뀜 (→ §3-2)
- **구획 효과:** 칸 크기가 같아도 **선 위치**를 옮기면 결과가 바뀜 (→ 이 절)

**원리적으로 제거 불가.** 대응은 "결과가 이 임의성에 흔들리지 않는지 확인":

> 검증 기준은 `m_i`가 정확한지가 **아니라**, **배치 A vs B의 순위가 뒤집히지 않는지**다.

**민감도 절차:** 격자 원점을 반 칸 이동시켜 전체 파이프라인 재실행 → 최적 배치 순위 유지 여부 확인.
순위가 바뀌면 격자가 너무 성기다는 신호.

※ "상대 비교 목적"이라는 프로젝트 원칙이 **검증 방법까지 정해주는 사례.** 절대값 정확도를
요구했다면 MAUP은 답이 없는 문제였을 것.

### 3-5. 빈 칸 처리 ★신규 (실무에서 더 자주 터짐)

| 상황 | 처리 |
|---|---|
| **질량 있음 + 도로 없음** | ⚠ 트립이 조용히 소멸 → 총량 불일치. **질량을 가장 가까운 도로 보유 칸으로 이관**, 또는 트립 배치 시 인접 칸 도로를 후보에 포함. 핵심은 "질량이 소리 없이 증발하지 않게" |
| **도로 있음 + 질량 없음** (공원·강) | 질량 0이 물리적으로 맞음. 출발지는 아니지만 **통과 경로로는 사용됨** |

---

## §4. 존 질량 계산

```
m_i = Σ (건물 바닥면적 × 층수)     for 존 i 안의 건물
```
= 사실상 **건물 부피**.

**왜 부피인가:** 큰 건물엔 사람이 많고, 사람이 많으면 통행이 많이 생긴다. 아파트 단지는 사람을
많이 내보내고, 오피스빌딩은 많이 끌어들인다. 인구 통계를 직접 구할 수 없으므로 **건물 부피를
인구·일자리의 프록시**로 사용. → FSM(4단계 모델)의 Trip Generation 단계에 해당.

---

## §5. 기준 총 교통량 N\* — KTDB 접지 ★신규 (v1에서 변경)

### 5-1. 왜 사용자 입력을 그대로 쓰면 안 되는가

v1에서는 총 차량 수를 사용자가 입력했으나 **두 가지 문제:**

1. **검증이 순환한다.** N을 사용자가 정하면 시뮬레이션 교통량이 N에 거의 비례 → VDS와 비교해도
   "사용자가 방금 넣은 숫자"를 재확인할 뿐.
2. **더 심각 — N이 정체 존재 여부를 결정한다.** N은 스케일 손잡이가 아니라 연구 대상 현상의
   존재 스위치:
   - N 너무 작음 → 전 구간 자유흐름 → **정체 없음** → spillback 없음 → RL이 배울 대상 소멸
   - N 너무 큼 → 전 구간 격자정체 → 어느 배치든 다 나쁨 → **배치 간 차이 소멸**

   프로젝트 전체가 "예측 가능한 정체 위에서 RL이 우회를 학습"이라는 논지 위에 있는데, 그 성립
   여부를 사용자 입력에 맡기는 구조가 됨.
3. **분포 검증도 오염.** 정체가 끼면 병목에서 흐름이 포화되고 우회가 발생해 분포 자체가 바뀜.
   → "스케일은 틀려도 모양은 맞다"가 성립하지 않음.

### 5-2. 해결 구조 — 노브를 없애지 말고 자리를 옮긴다

```
실제 총 교통량  N  =  N*(시스템 산출 기준값)  ×  배율(사용자 입력, 기본 1.0)
```

**UI 표시 예:**
```
교통 수요:  [====|========]  1.0배
현실 기준(KTDB 전국평균 보정) 대비
→ 약 3,600 통행/시
```

세 가지를 동시에 달성:
- **기본값이 방어 가능:** "왜 3,600대냐" → "국토부 통계에서 산출"
- **극단값이 극단으로 보임:** 5.0배 입력 시 "현실의 5배"로 표시 → 결과 해석 왜곡 방지
- **시나리오 탐색 유지:** "교통량 1.5배면 이 배치가 버티나"는 정당하고 가치 있는 질문

※ latency 설계에서 `P_tx`를 α 역산으로 소거하고 임의성을 방어 가능한 `d_edge`로 옮긴 것과
**동일 구조.** 방어 불가능한 자리(사용자가 찍은 절대 대수) → 방어 가능한 자리(통계 기준값 +
명시적 배율).

### 5-3. N\* 산출 — KTDB 기반 (기본 경로)

**VDS는 지역 의존적으로 없을 수 있으므로 KTDB가 주 경로.** 표준노드링크가 모든 링크에 `road_rank`를
달고 있고 KTDB가 등급별 AADT를 주므로, **지도를 불러온 순간 등급 구성만 세면 기준값 산출 가능.**

**계산 예시:**

| 도로 등급 | 총 연장 | KTDB AADT (대/일) |
|---|---|---|
| 일반국도(103) | 10 km | 29,000 |
| 시도(106) | 40 km | 13,200 |
| 구도(108) | 100 km | 2,640 |

**1) 하루 총 주행거리 (링크별 AADT × 길이 합):**
```
 10 km × 29,000 =   290,000
 40 km × 13,200 =   528,000
100 km ×  2,640 =   264,000
────────────────────────────
합계            = 1,082,000 대·km/일
```

**2) 첨두 1시간으로 환산 (K계수, 도시부 ≈ 0.10):**
```
1,082,000 × 0.10 = 108,200 대·km/시   ← 첨두 목표치
```

**3) 시뮬레이션 반복 보정:**
```
N₀ = 1,000 통행/시로 1회 실행 → 결과 30,000 대·km/시
γ = 108,200 ÷ 30,000 ≈ 3.6
N ← 1,000 × 3.6 = 3,600 통행/시
→ 2~3회 반복 수렴
```

**⚠ 반복이 필요한 이유 (비선형성):** 자유흐름에서는 N을 2배 하면 교통량도 대략 2배지만, 정체가
끼면 도로 통과량(veh/h)이 용량에서 **포화**됨. N을 2배 해도 병목 통과량은 1.2배만 늘고 대신 뒤에
줄이 길어짐. → γ를 한 번 곱하고 끝내면 목표 미달.

### 5-4. VDS가 있을 때 — 국소 보정 (업그레이드 경로)

VDS가 있으면 KTDB 전국 평균에서 **그 지역 실제값으로 끌어당기는 국소 보정** 수행. 추가로 VDS는
시간대별 데이터이므로 **첨두/비첨두 N\*를 각각 캘리브레이션 가능** (K계수 가정을 대체).

**⚠ (a) volume만 맞추면 안 됨 — 밀도 모호성.** 정체 구간에선 같은 통과량(veh/h)이 두 상태에
대응함: ①차가 적당히 있고 빠르게 통과 ②차가 꽉 차서 느리게 기어감.
```
우리 BS 부하 ρ에 들어가는 것은 통과량이 아니라 "그 구역에 지금 차가 몇 대"(밀도)
```
→ 통과량만 맞추면 밀도가 엉뚱하게 나옴. **VDS의 occupancy(점유율)를 함께 맞춰야** 모호성 해소.
→ 캘리브레이션 목표 = **volume + occupancy 동시**

**⚠ (b) N의 단위 정의 — Little's Law.**
```
동시 주행 차량 수 = 통행 발생률 × 평균 통행 시간
```
평균 통행 시간이 정체에 따라 변하므로 둘은 고정 비율이 아님.
→ **N을 "시간당 발생 통행 수"로 정의** (SUMO 입력·VDS 단위와 정합). 동시 차량 수는 시뮬레이션
   결과로 산출.

※ 친구 문서가 `δ = Q/v`를 Little's Law로 인용한 것은 오기 — 그것은 교통류 기본식 `q = k·v`.
  Little's Law는 **여기(발생률↔동시대수 변환)에서** 실제로 필요함.

**⚠ (c) VDS의 공간 편향.** VDS는 고속도로·간선에만 존재. 거기서 맞춘 N을 전체에 적용하면
"radiation이 간선 대 이면도로 비율을 제대로 맞혔다"는 가정이 숨어 들어감. 원리적 제거 불가 →
KTDB 등급별 비율로 교차 점검(§9).

### 5-5. 캘리브레이션 소스 우선순위 및 근거 등급

| 순위 | 소스 | 근거 등급 | UI 표시 | 가용성 |
|---|---|---|---|---|
| 1 | VDS 실측 (해당 지역, 시간대별) | [실측 근거] | "실측 보정됨" | 지역 의존 |
| 2 | **KTDB 전국평균 AADT** | [문헌 근거] | "전국 통계 기준" | **항상** |
| 3 | 없음 (사용자 입력만) | [설계 가정] | "미보정" | — |

**⚠ KTDB 기준값의 정직한 한계:** KTDB AADT는 **전국 등급별 평균**. 같은 "시도"라도 도심과 지방
소도시는 실제 교통량이 몇 배 차이. 즉 "이 등급 구성의 도로망이라면 대략 이 정도"라는 근사이지
그 지역의 실제 교통량이 아님.
→ 방어: 목적이 절대 교통량 예측이 아니라 **배치 비교**. 배치 A와 B를 **같은 수요 조건**에서
  비교하는 것은 기준값이 전국 평균이어도 유효.

### 5-6. ⚠ 절대 금지 — 등급별로 다시 맞추기

KTDB 표에 등급별 AADT가 있으므로 "도로 등급마다 각각 맞추면 더 정확하지 않나?"라는 유혹이 생김.
**금지.** 친구 설계의 `α_rank`가 저지른 것과 동일한 오류가 됨:

도로 등급 효과는 **이미 두 군데**에 반영되어 있음 —
1. 표준노드링크의 차로수·제한속도가 SUMO 배정에서 **링크 용량**으로 작동
2. radiation이 건물 분포로 **수요 방향**을 결정

여기에 등급별 보정을 또 곱하면 **같은 효과를 두 번 세는 이중계산** (β/A_seg 역할 분리 문제와 동일).

> **원칙: KTDB에서 가져오는 자유도는 딱 1개 — 총량 스칼라. 분포는 손대지 않는다.**
> 등급별 비율은 **사후 검증 지표**로만 사용 (§9).

---

## §6. Radiation Model — 목적지 결정 ★핵심

### 6-1. 아이디어: "거리"가 아니라 "사이에 뭐가 있나"

일반적 직관은 "가까울수록 많이 간다"(gravity model). 실제 행동은 조금 다름:

> 집에서 2 km 떨어진 식당에 갈지는 **거리보다 "가는 길에 다른 식당이 몇 개 있나"**에 달렸다.
> 중간에 30개 있으면 거기서 해결하고, 하나도 없으면 2 km를 간다.

이것이 **개입 기회(intervening opportunities)** 개념. 물리에서 이름을 따옴 — 사람이 출발지에서
사방으로 **뻗어나가다가(복사, radiation)** 조건 맞는 첫 목적지에 **붙잡힌다(흡수)**. 중간에 후보가
많으면 멀리 못 가고 붙잡힘.

### 6-2. 수식

```
                    m_i × m_j
P(i→j) = ─────────────────────────────────
          (m_i + s_ij) × (m_i + m_j + s_ij)

T_ij = O_i × P(i→j)
```

| 기호 | 의미 | 우리 구현 |
|---|---|---|
| `m_i` | 출발 존 질량 | 건물 부피 합 (§4) |
| `m_j` | 도착 존 질량 | 건물 부피 합 |
| `s_ij` | **i에서 봤을 때 j보다 가까운 존들의 질량 합** (i, j 자신 제외) | 반경 내 존 질량 합 |
| `O_i` | i의 총 유출 통행량 | `N × m_i / Σm` (§3의 ③) |

```
s_ij = Σ_k  m_k  ·  [ d_ik < d_ij ]      (k ≠ i, j)
```

**⚠ 구현 팁 — 누적합 룩업:** 출발지 i마다 다른 존들을 거리순 정렬 + 질량 누적합 precompute →
`s_ij = (거리 d_ij까지 누적 질량) − m_i − m_j`로 O(1) 조회.
※ `A_seg`를 `(segment_id, station_id)` 키로 사전계산한 패턴과 동일.

**⚠ 정규화 필수:** 확률 합이 1이 되지 않음 (무한 평면을 가정해 유도된 모델). 실제 구현에서는
출발지별로 `Σ_j P(i→j) = 1`이 되도록 정규화해야 함. **코드로 옮길 때 빠뜨리기 쉬운 부분.**

### 6-3. 계산 예제

**예제 도시** — 동서 도로 하나에 지역 4개:

```
   A                C                    B              D
   ●────────────────●────────────────────●──────────────●
  0m              500m                1500m          2000m
아파트단지        작은 상가            도심 오피스        공원
  m=100            m=50                 m=200          m=10
```
질량 합 = 360, 총 차량 = 1,000대 가정.

**존별 출발 대수 (`O_i = 1000 × m_i/360`):**

| 존 | 계산 | O |
|---|---|---|
| A | 1000 × 100/360 | **278대** |
| C | 1000 × 50/360 | **139대** |
| B | 1000 × 200/360 | **556대** |
| D | 1000 × 10/360 | **28대** |

**A에서 출발하는 278대의 목적지 계산:**

```
A → C (500m):   s = 0        (500m보다 가까운 존 없음)
                P = (100×50) / [(100+0)(100+50+0)] = 5,000/15,000 = 0.333

A → B (1500m):  s = 50       (C가 500m에 있음 → 포함)
                P = (100×200) / [(100+50)(100+200+50)] = 20,000/52,500 = 0.381

A → D (2000m):  s = 250      (C=50 + B=200)
                P = (100×10) / [(100+250)(100+10+250)] = 1,000/126,000 = 0.008
```

정규화 후 (합 0.722 → 1.0):

| 목적지 | 확률 | 차량 수 |
|---|---|---|
| A → C (상가) | 46.2% | **128대** |
| A → B (오피스) | 52.7% | **147대** |
| A → D (공원) | 1.1% | **3대** |

### 6-4. 이 모델이 잡아내는 것 3가지

**① 그늘 효과 — 중간에 뭐가 생기면 먼 곳 흐름이 준다**

중간 상가 C가 대형 쇼핑몰로 성장해 질량 50 → 200이 되면:
```
s_AB = 200
P(A→B) = (100×200) / [(100+200)(100+200+200)] = 20,000/150,000 = 0.133
```
**0.381 → 0.133 (약 1/3).** A와 B의 크기도, 둘 사이 거리도 불변인데 흐름이 급감.
거리만 보는 모델은 이를 원리적으로 잡을 수 없음.

**② 방향 비대칭 — `T_ij ≠ T_ji`**

| 방향 | 확률(정규화) |
|---|---|
| A → B | **52.7%** |
| B → A | **48.1%** |

`s_ij`가 출발지 중심이라 발생. B 주변엔 갈 만한 다른 곳(C, D)이 있어 B에서 나선 사람이 중간에
붙잡히지만, A 주변엔 없어 A에서 나서면 멀리까지 감.
→ **출퇴근 방향 쏠림이 억지로 넣지 않아도 자동 생성.**

**③ 조정할 파라미터가 없다 (parameter-free)**

질량과 거리만 넣으면 끝. 지역별로 맞춰야 할 계수가 없음 → 심사에서 "그 숫자는 어디서 나왔나요?"
라는 질문 대상 자체가 없음.

### 6-5. 확장 슬롯 — Radiation Model with Selection (λ)

**기본형의 한계:** 조절 손잡이가 없어 "사람이 평균 얼마나 멀리 가는가(특성 거리)"가 지형의 기회
밀도만으로 자동 결정됨. 기본 모델은 카운티 스케일(~10 km)에 맞춰 유도되어 **도시 내부(~1 km)
스케일과 불일치.** 이것이 "radiation은 도시 내부에서 약하다"의 진짜 원인.

**RMS (Simini, Maritan, Néda, PLoS ONE 2013):** 제안 수용 확률을 `(1−λ)`배로 축소 (λ ∈ [0,1]).
해석: 사람이 더 나은 제안을 확률 (1−λ)로만 받아들임 / 전체 기회 중 (1−λ)만 인지함.

```
기본형:   P_>(a) = 1 / (a + 1)
RMS:      P_>(a) = (1 − λ^(a+1)) / [ (a + 1)(1 − λ) ]

여기서 a = m_i + s_ij  (j보다 가까운 기회 수)
       P(i→j) = P_>(a) − P_>(a + m_j)
```

- **λ = 0** → 기본형으로 정확히 환원
- **λ > 0** → `P_>(a)` 중앙값이 큰 a 쪽으로 이동 → **특성 통행거리 증가**
- **λ = 특성 통행거리 다이얼.** 도시가 작으면 낮게, 광역이면 높게.

**⚠ λ 값 함정:** US 카운티 적합값 λ ≈ 0.999988. "λ는 항상 1 근처"라는 뜻이 **아님** — 인구를
일자리 프록시로 쓰며 `λ_pop = λ_jobs^c` (c ≪ 1)가 되어 프록시 λ가 1로 쏠린 것.
→ 우리도 건물 부피를 프록시로 쓰므로 λ는 1 근처에서 미세 조정하는 형태. **절대값이 아니라 그것이
만드는 특성거리를 보고 맞춤.**

**형제 모델:** Extended radiation model (Yang et al. 2014) — 생존분석 틀에서 개입기회 `s_ij`의
영향력을 지수 α로 조절. OD 닫힌식이 더 깔끔해 도시 내부용으로 실무 구현이 잦음. RMS(λ)와
extended(α)는 "특성거리 손잡이 하나"라는 점에서 동일 → 둘 다 후보.

**구현 방침:**
- **MVP:** 기본 radiation(λ=0)으로 시작, 파라미터-프리 서사 유지
- **슬롯:** distribution을 `gravity / radiation / radiation+selection` **교체형 플러그인**으로 설계
  (warm-start를 greedy/random 슬롯으로 뺀 것과 동일 철학)
- λ(또는 α)는 [설계 앵커]로 노출. `d_edge`를 "엣지 반경"으로 방어한 것과 동일 성격 —
  자유 노브가 아니라 **지역 스케일로 고정되는 앵커**

---

## §7. OD → 개별 차량 (비집계, disaggregation)

**⚠ 핵심 오해 방지: "A에서 B로 147대"는 147대가 같은 지점에서 출발한다는 뜻이 아니다.**

A는 500 m × 500 m **면적**이고 그 안에 도로가 여럿 있음. 147대는 **A 안의 여러 도로에 흩어져서**
출발하고, 도착도 B 안 여러 도로로 흩어짐.

```
존 A (500m × 500m)          존 B
┌──────────────┐            ┌──────────────┐
│ 🚗    🚗     │            │      🚗   🚗 │
│    🚗    🚗  │  ────→     │  🚗       🚗 │
│ 🚗   🚗   🚗 │            │    🚗   🚗   │
└──────────────┘            └──────────────┘
```

- 어느 도로에서 출발할지는 **무작위 추출 + 건물 많은 쪽에 가중치**
- SUMO **`od2trips`** 가 담당. 출력 = 차량 한 대씩의 "출발 도로 → 도착 도로" 목록

**※ 반경 `d_ij`인 원은 OD 영역이 아님.** `s_ij`를 세기 위한 **측정용 영역**일 뿐, 출발/도착과 무관.

---

## §8. 경로 배정 — 정체가 발생하는 곳

### 8-1. 엣지 교통량 = 그 엣지를 지나는 모든 트립의 합

예제 도로를 세 구간으로 나눠 실제 집계:

```
   A ──구간1── C ──구간2── B ──구간3── D
```

**구간2(C~B)를 지나는 차량:**

| 트립 | 대수 |
|---|---|
| A→B (동쪽으로 통과) | 147 |
| A→D (통과) | 3 |
| C→B | 31 |
| C→D | 1 |
| B→A (서쪽으로 통과) | 267 |
| B→C | 229 |
| **합계** | **678대** |

| 구간 | 총 통행량 | 성격 |
|---|---|---|
| 구간1 (A~C) | 653대 | 붐빔 |
| **구간2 (C~B)** | **678대** | **가장 붐빔 ← 병목** |
| 구간3 (B~D) | 91대 | 한산 |

**★ 핵심 관찰:** 구간2가 가장 붐비는데 그 차량 대부분(147+267 = 414대)은 **구간2가 목적지가
아니라 통과 중.** 주거지 A와 직장 B 사이를 오가는 통근 차량이 그 사이 구간에 쌓임.

> 기존 확정 원칙 **"혼잡한 엣지는 OD 종점이 아니라 통과하는 병목"** 이 숫자로 확인됨.
> 그리고 이 병목은 손으로 지정한 것이 아니라 **건물 분포에서 자동 유도**됨.
> → RSU를 교차로에 배치하는 설계 서사와 직결.

### 8-2. 반복 배정 (Wardrop User Equilibrium)

차가 많아지면 그 구간이 느려지고, 느려지면 일부가 **다른 길로 우회**. SUMO는 이 과정을 반복해
"모두가 자기 최선 경로를 골라 더 이상 바꿀 이유가 없는 상태"를 찾음 (`duaIterate`).
= Wardrop 제1원리(1952) 의 반복 근사. 실제 운전자 행동과 동일한 원리.

---

## §9. 검증 — 친구 설계 데이터의 자리

여기까지는 전부 **계산으로 만든 합성 교통.** "현실과 비슷한가"를 확인해야 함.

### 9-1. 검증 수단

| 수단 | 내용 | 순환 여부 |
|---|---|---|
| **VDS 홀드아웃** | VDS 지점을 나눠 일부만 캘리브레이션에 사용, 나머지는 검증 전용 | 비순환 ✓ |
| **잔차 패턴** | N\* 하나(자유도 1)로 맞췄으므로 지점 K개면 K−1 자유도가 남음. 지점별 과대/과소 패턴이 **radiation 분포 품질**을 말해줌 | 비순환 ✓ |
| **occupancy** | volume으로 맞췄다면 occupancy는 독립 검증 지표 | 비순환 ✓ |
| **KTDB 등급별 비율** | "고속도로 대비 이면도로 ≈ 5%" 같은 등급 비율과 대조. 이면도로에 간선만큼 흐르면 이상 신호 | 비순환 ✓ (별개 출처) |

**⚠ 검증 순환 주의:** 같은 데이터로 맞추고 같은 데이터로 검증할 수 없음. VDS로 N\*를 정했으면
"VDS와 잘 맞네요"는 당연한 결과이지 검증이 아님. → 위 4가지로 분리.

### 9-2. 검증의 기준선

정확히 일치할 필요 없음. **자릿수와 순서(순위)가 맞으면 성공.** 목적은 정확한 교통량 예측이
아니라 배치 비교.

### 9-3. ⚠ 과적합 금지

VDS 지점이 몇 개뿐인데 거기에 OD를 강하게 맞추면, 관측 지점만 정확하고 나머지가 뒤틀린 OD가 됨.
radiation이 주던 깨끗한 구조가 망가짐.

> **원칙: 뼈대는 항상 합성(radiation), 실측은 스케일 보정 + 검증용.**
> 실측을 강한 제약이 아니라 **부드러운 앵커**로 사용하고, 보정된 OD가 radiation prior에서 크게
> 벗어나지 않는지 확인.

---

## §10. SUMO 운영 설정 ★신규

### 10-1. 시계가 세 개 — 혼동 정리

| | 무엇 | 예 |
|---|---|---|
| **시뮬레이션 시간** | 시뮬레이터 내부 가상 시계 | "시뮬레이션 안에서 08:15" |
| **실제 시간(wall-clock)** | 컴퓨터가 실제로 쓴 시간 | "돌리는 데 3분 걸림" |
| **차량 속도** | 가상 시계 기준 물리량 | "50 km/h" |

**차량 속도는 시뮬레이션 시간 기준으로 정의됨** → 계산이 빠르든 느리든 차는 여전히 50 km/h.
(영화를 2배속으로 봐도 등장인물의 작품 내 달리기 속도는 불변.)

"시뮬레이션 속도"라 부르는 것은 실제로 **두 개의 다른 손잡이:**

| 손잡이 | 정체 | 영향 |
|---|---|---|
| **`--step-length`** | 시간 해상도. 기본 **1.0초** | **정밀도 손잡이.** 0.1로 주면 계산량 10배, 움직임 매끄러움 |
| **GUI `Delay (ms)`** | 재생 지연 (사람이 보라고) | **계산 결과에 영향 없음.** headless `sumo`/libsumo에는 없음 |

### 10-2. lockstep — 연산이 늦어도 오류 없음

**우려:** "SUMO가 자기 시계로 돌아가는데 우리 계산이 늦으면 놓치지 않나?"
**답: 그런 구조가 아님.** libsumo는 한 발씩 번갈아 딛는 lockstep:

```python
while step < N:
    libsumo.simulationStep()        # ① SUMO가 딱 1스텝 전진하고 멈춤
    pos = get_all_positions()       # ② 우리 코드: 위치 읽기
    latency = compute_latency(pos)  # ③ 우리 코드: 신호·지연 계산 (오래 걸려도 됨)
    step += 1                       # ④ 다시 ①로
```

- ②③ 동안 **SUMO는 멈춰서 기다림.** `simulationStep()`을 호출해야만 전진.
- 계산이 10배 무거워져도 **결과는 완전히 동일**, 실제 소요 시간만 10배.
- "프레임 드랍"·"동기화 깨짐"은 **원리적으로 발생 불가.**

> **시뮬레이션 속도는 우리가 설정하는 입력이 아니라, 계산량에 따라 결정되는 결과.**

**실시간이 문제 되는 유일한 지점:** 프론트엔드 WebSocket 실시간 스트리밍. 1배속 재생 중 한 스텝
계산에 3초 걸리면 화면이 끊김. → **화면 문제이지 계산 오류 아님.** 해결: 궤적을 미리 캐시하고
재생 (§11).

**⚠ 단, step-length가 `A_seg`를 흔들 수 있음:** 1초 스텝이면 50 km/h 차량이 **한 스텝에 14 m
순간이동.** 거리 기반 경로손실엔 무해(500 m 거리에서 14 m는 무시 가능)하나, **건물 차폐는 다름** —
차가 건물 뒤를 스쳐 지나는 순간을 건너뛰면 차폐가 **깜빡거림.**
→ 대응: **교통 스텝과 통신 평가 주기 분리.** 교통 1초, 통신 1초 평가하되 필요 시 타깃 차량만
0.1초 보간. ("타깃 차량 고해상도 / 나머지 저해상도" 캐시 설계와 동일 맥락)

### 10-3. 정체가 생겼다 풀리는 패턴을 보려면 — 4가지 필요조건

공간 규모는 필요조건이지만 **가장 먼저 걸리는 병목이 아님.** 중요도 순:

**① 수요가 시간에 따라 변해야 한다 (가장 중요)**
정체는 도착률이 용량을 넘을 때 쌓이고, 용량 아래로 내려갈 때 풀림. 수요가 상수면:
- 수요 < 용량 → 끝까지 안 막힘
- 수요 > 용량 → 끝까지 쌓이기만 함 (풀리는 것을 못 봄)

→ **"생겼다 풀린다"를 보려면 수요가 올라갔다 내려가야 함.** 지도를 아무리 키워도 수요가 상수면
   이 패턴은 안 나옴. **공간 문제가 아니라 시간 프로파일 문제.**
   → §14의 시간대 수요 프로파일이 이 질문의 핵심 답.

**② 진짜 병목이 존재해야 한다 — 신호등 확인 필수**
도시 정체의 주원인은 도로 구간이 아니라 **신호 교차로.** 신호가 없으면 교차로 통과 용량이
비현실적으로 커져 수요를 넣어도 정체가 안 생김. 표준노드링크에 신호 주기 정보가 없을 가능성이
크므로 **`netconvert`가 기본 신호를 자동 생성하도록 설정.**
→ "정체가 안 생기네?"의 원인은 대개 이것.

**③ 큐가 자랄 공간이 있어야 한다**
```
정체 차량 간격 ≈ 7.5 m/대  (차량 5 m + 최소 간격 2.5 m)
100대 → 750 m
200대 → 1.5 km
```
**spillback**(줄이 상류 교차로를 넘어 역류)이 RL의 핵심 현상 → 링크 길이가 큐 길이와 비슷한
규모여야 관측 가능. 링크 200 m인데 큐 750 m면 즉시 상류로 넘어감. 지도가 너무 작으면 큐가
지도 경계에 닿음.

**⚠ 함정 — teleport:** SUMO는 차량이 너무 오래 멈춰 있으면(기본 **300초**) "막혔다"고 판단해
**순간이동시킴.** 정체 연구에서 이게 켜져 있으면 정체가 인위적으로 해소되어 결과가 무효화됨.
→ **`--time-to-teleport -1`로 비활성화** 또는 충분히 증가. 초심자가 가장 많이 당하는 함정이며
   우리 목적을 정면으로 훼손. (버전별 기본값 확인 필요)

**④ 우회로가 있어야 한다**
규모보다 **구조** 문제. 일직선 도로 하나면 아무리 길어도 RL이 배울 것이 없음(선택지 없음).
**평행 경로가 최소 2~3개 있는 격자형 도시**여야 "이 길 막히니 저 길로"가 성립.
→ 지도 선정 기준은 "넓이"가 아니라 **"경로 대안이 있는가".**

**⑤ 시간 창과 예열**
- t=0에는 도로가 비어 있어 비현실적 → **예열(warm-up) 15~30분** 후부터 측정
- 정체 한 사이클(형성→확산→해소)이 현실에서 30분~2시간 → **시뮬레이션 창 1~3시간**

**비용 추정:**
```
N* ≈ 3,600 통행/시, 평균 통행시간 10분
→ 동시 주행 차량 = 3,600 × (1/6) ≈ 600대   (Little's Law)
→ 1초 스텝 × 1시간 = 3,600 스텝 × 600대 ≈ 220만 레코드
→ 레코드당 ~20 byte → 수십 MB (메모리 상주 가능)
```
**교통은 한 번만 실행 후 캐시** → SA가 배치 100개를 평가해도 이 비용은 1회만 발생.

---

## §11. 최종 산출물 — 두 갈래 사용

```
SUMO 결과: 차량마다 (시각, 도로, x·y 좌표, 속도)
   │
   ├─ [통신 레이어] 개별 좌표 그대로 사용
   │     → 차량–기지국 3D 거리 · A_seg → SINR → latency  (타깃 차량)
   │     ※ "이 도로에 100대"로는 계산 불가. 같은 도로 위 차량도
   │        기지국까지 거리·차폐가 제각각이라 개별 좌표 필수.
   │
   ├─ [통신 레이어] 개별 좌표로 best-SINR 기지국 배정
   │     → 기지국별 머릿수 → ρ → L_queue
   │     ※ 엣지 교통량을 직접 큐에 넣지 않음. 개별위치 → 배정 → 기지국별 집계.
   │
   └─ [교통 레이어] 엣지 단위 집계
         → 교통량·속도 → 경로 비용 · 병목 판정 · RL flow field
```

**성능 원칙: "교통은 한 번, 통신 평가는 여러 번."**
교통 시뮬레이션은 1회 실행 후 궤적을 통째로 저장. 기지국 배치를 100가지로 바꿔 시험할 때 교통은
불변이므로 SUMO 재실행 없이 저장된 좌표로 신호·지연만 재계산.

※ load-coupling OFF 기준에서 배치 swap이 궤적을 바꾸지 않으므로 **궤적 캐시는 배치와 독립.**
  ON으로 전환 시 재검토 항목.

---

## §12. 친구 설계(TFR-CPA) 통합 — 채택/기각 내역

### 12-1. 역할 분담 원칙

> **생성(generation) = radiation model / 접지·검증(grounding) = 친구 설계의 데이터 계층**

두 설계가 같은 "교통 환경" 슬롯을 독립적으로 설계했으므로, 빌드 전 역할 합의 필요.

### 12-2. 채택 항목

| 항목 | 원래 역할(친구 설계) | 우리 파이프라인에서의 역할 |
|---|---|---|
| **표준노드링크** | 그래프 구축 기반 | **OSM 대체 → SUMO 네트워크.** 차로수·자유속도·등급 품질이 우수하고 배정 품질에 직결. 순수 이득 |
| **VDS/ITS 실측 (Layer 1/2)** | BFS 전파의 시드 | **N\* 국소 보정 + 홀드아웃 검증 앵커** (§5-4, §9) |
| **KTDB 등급별 AADT** | α_rank 감쇠 계수 | **기준 총량 N\* 산출의 주 경로** (§5-3) + 등급별 비율 사후 검증 (§9). **검증 지표에서 캘리브레이션 앵커로 승격** |

### 12-3. 기각 항목 — Layer 3 (BFS 용량 비례 전파)

**기각 사유 (심각도 순):**

1. **⚠ 헤드라인 주장이 자기모순 — α가 유량 보존을 깬다.**
   ```
   Q_k = Q_in × (C_k / ΣC)   ← split만 하면 Σ = Q_in, 보존 ✓
   Q_k ×= α_rank              ← α < 1 이므로 ΣQ_out < Q_in, 보존 깨짐 ✗
   ```
   §1 표에서 "도로 등급 단순 감쇠 → 교차로 유량 비보존"이라 비판한 것을 α_rank로 동일하게 저지름.

2. **공급(capacity)으로 흐름을 나눔 — 수요와 반대 방향.**
   용량은 도로가 얼마나 실어나를 수 있나지, 차가 어디로 가고 싶나가 아님. **넓지만 아무도 안 가는
   도로**를 교통량 많은 도로로 판정. supply-shaped이지 demand-shaped가 아님.

3. **국지 발생 교통이 없음.** 간선의 측정 유량을 아래로 흘려보내기만 함. 이면도로 교통의 상당수는
   거기 사는 사람의 국지 발생인데 그 항이 없음. 시드에서 멀어질수록 α<1이 매 홉 곱해져
   **기하급수적으로 0에 수렴** → "간선에서 먼 주거지 이면도로 = 교통량 0"이라는 알고리즘 아티팩트.
   ⚠ spillback이 흥미로워지는 지점이 정확히 "간선이 막혀 이면도로로 새는" 상황인데, 이면도로가
   비면 그 시나리오 자체가 생성 불가.

4. **등급 효과 이중계산.** `C = 차로수 × 자유속도`가 이미 등급에 단조 증가하는데 `α_rank`도 등급에
   단조. C로 한 번, α로 또 한 번 → β/A_seg 역할 분리 문제와 동일 계열.

5. **OD를 만들지 않아 동적 시뮬레이션·RL과 부정합.** 출력이 링크별 veh/h(정적 밀도장)에서 끝남.
   SUMO 마이크로시뮬레이션과 라우팅 RL은 **OD(트립)**이 있어야 작동. 정적 밀도장은:
   - 자라지도, 역전파되지도, 상류에 전조를 남기지도 않음 → **선행 신호 부재**
   - 타깃이 우회해도 배경이 반응하지 않음 → RL이 배우는 것이 "교통 동역학"이 아니라 "고정 벽 피하기"
   - radiation OD 위에 얹으면 **수요 이중계산**

   ※ 부차: `δ = Q/v`를 Little's Law로 인용한 것은 오기(교통류 기본식 `q = k·v`).
     `v_freeflow`로 밀도 계산 시 정체 구간에서 실제 밀도를 과소추정 — 정작 BS 부하가 중요한 곳이
     정체 구간임.

### 12-4. 친구에게 설명할 프레이밍

> Layer 3를 버리는 것이 아니라, **친구가 해결하려던 문제(미계측 구간 채우기)를 radiation이 더
> 근본적으로 — 수요에서부터 — 해결**하는 것. 친구 설계의 데이터 계층(Layer 1/2)과 표준노드링크는
> 우리 파이프라인의 **접지 담당으로 그대로 승격**됨.

### 12-5. 왜 radiation이 우리 목적에 부합하는가

목적 = "교통에 패턴이 있고, 정체가 예측 가능할 것". 이것이 성립하려면:

| 요건 | 내용 | radiation | TFR-CPA |
|---|---|---|---|
| **(a) 재현성** | 같은 수요면 같은 곳이 막힘 | ✓ | △ (아무것도 안 변해서 재현) |
| **(b) 선행 신호** | 정체가 시간을 두고 쌓이고 전조가 상류에 관측됨 | ✓ | **✗ 정적 밀도장** |
| **(c) 구조적 변이** | 매번 똑같으면 경로 암기가 됨 | ✓ (OD 재샘플링) | ✗ |
| 현실 병목 일치 | | 미검증 → **실측 앵커로 검증** | ✓ (측정 기반) |

> **친구 설계는 정체가 어디 있는지(where)를 알려주고, radiation은 왜 생기는지(why)를 알려준다.**
> RL이 정책을 학습하려면 why가 필요하고, where는 그 why가 현실과 맞는지 검증하는 데 쓴다.

---

## §13. 파라미터 근거 등급 요약

| 파라미터 | 값/방식 | 근거 등급 | 비고 |
|---|---|---|---|
| 도로망 속성 (차로수·속도·등급) | 표준노드링크 | [표준 유도] | 국가 표준 데이터 |
| 존 질량 = 건물 부피 | footprint × 층수 | [설계 가정] | 인구·일자리 프록시 |
| 격자 크기 | 200~500 m (미확정) | [설계 파라미터] | 민감도 분석으로 방어 |
| 격자 원점 | 임의 | [설계 가정] | MAUP — 순위 안정성으로 검증 |
| 걸친 건물 배정 | 대표점 (대형은 면적분할) | [설계 가정] | 질량 보존 필수 |
| OD 분포 | Radiation model | [문헌 근거] | parameter-free |
| λ (RMS) | 미사용 (슬롯만) | [설계 앵커] | 특성 통행거리 |
| 기준 총량 N\* | KTDB AADT 기반 산출 | [문헌 근거] | 전국 평균의 한계 명시 |
| K계수 (일→첨두시) | 0.10 (도시부, 미확정) | [설계 앵커] | VDS 있으면 대체 |
| 수요 배율 | 사용자 입력 (기본 1.0) | [사용자 시나리오] | 결과에 명시적 라벨 |
| 경로 배정 | Wardrop UE (duaIterate) | [표준 유도] | |
| step-length | 1.0 s (교통) | [설계 파라미터] | A_seg 안정성과 트레이드오프 |
| time-to-teleport | 비활성화 | [설계 결정] | 정체 연구 필수 |

---

## §14. 다음에 확정할 것 (Open Decisions)

### (A) ★우선순위 1 — 시간대 수요 프로파일
§10-3의 ①에서 승격. **"정체가 생겼다 풀린다"의 성립 여부가 여기 걸려 있음** — 수요가 상수면
공간 규모와 무관하게 이 패턴이 나오지 않고, 그러면 RL이 학습할 대상 자체가 없음.
- [ ] K계수 0.10 고정 vs 시간대별 곡선 생성
- [ ] 첨두 곡선 모양: 시작·정점·해소 시각과 피크 비율
- [ ] 첨두/비첨두 두 시나리오를 각각 어떻게 정의할지
- [ ] VDS가 있는 지역에서는 시간대별 실측으로 대체 가능 (§5-4)

### (B) 격자 크기 확정
- [ ] 대상 지역 평균 블록 크기·교차로 간격 측정
- [ ] BS/RSU 커버리지 반경 확정값과 비교 → 상한 결정 (칸 < 커버리지)
- [ ] 후보 범위(200~500 m) 중 확정, 또는 [설계 파라미터] 노출 + 민감도 범위 지정
- [ ] **격자 원점 민감도 테스트 절차 확정** (반 칸 이동 → 배치 순위 유지 확인, §3-4)
- [ ] 대형 건물 분할 임계값 (격자 면적의 5%?) 확정
- **결정 기준:** 하한 = radiation `s_ij` 통계 유효성 / 상한 = 배치 커버리지

### (C) 궤적 캐시 데이터 구조
- [ ] 저장 필드: 최소 `(vehicle_id, t, edge_id, x, y, speed)` — 추가 필요 필드?
- [ ] 저장 주기: 타깃 차량 고해상도 / 나머지 저해상도 이원화 (§10-2의 A_seg 깜빡임 대응)
- [ ] 저장 포맷: in-memory(numpy/pandas) vs 디스크(parquet/HDF5)
- [ ] 인덱싱: "시각 t에 특정 기지국 반경 내 차량" 조회 최적화 (시공간 인덱스 / 엣지별 버킷)
- [ ] 캐시 무효화: OFF 기준에서 배치와 독립. ON 전환 시 재검토

### (D) SUMO 네트워크 준비
- [ ] 표준노드링크 → SUMO 네트워크 변환 파이프라인 (netconvert 옵션)
- [ ] **신호등 자동 생성 설정 확인** (§10-3의 ②)
- [ ] `--time-to-teleport` 설정값 확정 및 버전별 기본값 확인
- [ ] 대상 지역 선정 기준: "경로 대안이 2~3개 있는 격자형" (§10-3의 ④)

### (E) N\* 캘리브레이션 구현
- [ ] KTDB 등급별 AADT 테이블 확보 및 road_rank 매핑
- [ ] 반복 수렴 종료 조건 (γ 변화율? 최대 반복 횟수?)
- [ ] VDS 유무 자동 감지 → 경로 분기 로직
- [ ] VDS 있을 때 volume + occupancy 동시 목적함수 설계
- [ ] 홀드아웃 분할 비율

---

## §15. 참고문헌

**Radiation model**
- Simini, González, Maritan, Barabási. *A universal model for mobility and migration patterns.*
  Nature 484, 96–100 (2012). arXiv:1111.0586
- Simini, Maritan, Néda. *Human Mobility in a Continuum Approach.* PLoS ONE 8(3):e60069 (2013).
  arXiv:1206.4359 — RMS, λ = 특성거리 다이얼
- Yang et al. (2014) — extended radiation model, 생존분석 기반 지수 α
- Masucci et al. 2013 (arXiv:1206.5735) — intra-city 스케일 한계 보고

**교통수요 프레임워크**
- Four-Step Model: Trip Generation → Distribution → Mode Choice → Assignment
- Wardrop (1952) user equilibrium; Beckmann (1956); Frank-Wolfe (1956)
- ODME: Willumsen (1978, entropy max), Van Zuylen (1977, information min)

**국내 데이터**
- 국토교통부 (2022). 전국 교통량 조사보고서 (KTDB) — 도로 등급별 AADT
- 도로용량편람 (KHCM, 2013) — 차로당 용량
- 국가교통정보센터 표준노드링크

**공간 분석**
- MAUP (Modifiable Areal Unit Problem) — Openshaw (1984)

**SUMO**
- od2trips, duarouter, duaIterate.py, netconvert
- libsumo (TraCI 대비 성능 우위)
